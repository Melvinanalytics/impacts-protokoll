# Worked disposition for the synthetic folders

This is an answer key for the [case](CONTEXT.md), not a second protocol authority or an executed migration.

| Source namespace | Item key | Resolvable location and applicable revision | Owned meaning or operation | Authoritative relationship home |
|---|---|---|---|---|
| `urn:impacts:example:cross-repository:customer` | `delivery-promise` | [`grundlagen/lieferzusage.md#definition`](fixtures/customer/grundlagen/lieferzusage.md#definition) at revision `c8` | The local delivery-promise meaning, with reported inputs and open availability premise. | The customer definition maintains its use of the estimator. |
| `urn:impacts:example:cross-repository:method` | `delivery-estimate` | [`skills/delivery-estimate/SKILL.md`](fixtures/method/skills/delivery-estimate/SKILL.md) at revision `m4` | The reusable estimate calculation and its input/output contract. | Customer and sales definitions maintain their dependent uses; the consumer inventory is derived navigation. |
| `urn:impacts:example:cross-repository:method` | `availability-check` | [`skills/availability-check/SKILL.md`](fixtures/method/skills/availability-check/SKILL.md) at revision `m4` | The separate operation that produces case-and-date availability evidence. | The sales definition maintains its evidence requirement. |
| `urn:impacts:example:cross-repository:sales` | `offer-delivery-commitment` | [`grundlagen/offer-terms.md#delivery`](fixtures/sales/grundlagen/offer-terms.md#delivery) at revision `s3` | Conditions for a delivery commitment. | The sales definition maintains its estimator, availability and customer-meaning relationships. |

The customer definition fits a Knowledge Bundle; accumulating offer instances would earn a Record Library only when observed. The method repository is a System Map with a reusable operation. The sales folder contains an existing declared Pipeline and historical Core Run. None of these facts earns a new Core Application. For each repository, the actual form-selection work report must still identify its growing units and accepted result.

The candidate map keeps source namespace `urn:impacts:example:cross-repository:method` and item key `delivery-estimate` separate from location and revision. The old tuple resolves to `skills/delivery-estimate/SKILL.md` at revision `m4`; the proposed tuple resolves to `skills/lead-time/SKILL.md` at candidate revision `m5`. Identity continuity is a proposal until the responsible source authority confirms namespace continuity and the move checks pass. First check destination collision; after authorized copy, compare count and hashes to establish only copy identity. Then check the customer relationship, installed targets, known scripts/jobs and the estimator's declared inputs and output against `m5`. Recheck the sales relationships against availability, promise and approval conditions. A semantic rule change, protocol upgrade or changed input/output contract follows its separate review path. No check in this fixture was actually run.

Retain the old path while any live consumer still needs it or the identified outside job remains unresolved, unless a supported compatibility path covers that use. If the declared search instead finds no specific outside lead, record its coverage and residual uncertainty. The responsible source owner then decides, within the required human authorization, whether to retire with the recovery path or maintain a stated compatibility period. This decision does not claim that every external consumer was found. If an update fails, restore the previous route from the retained source/revision. Inventory, collision checking, candidate copy and independent consumer preparation may continue while a specific lead is unresolved; retirement and the affected commitment cannot be claimed complete.

The estimator's `status: ok` establishes at most the declared calculation on its supplied inputs. It cannot supply missing availability evidence or a human decision. The older Run keeps its bound Application, inputs and v0.3.3 context; a v0.3.4 check does not validate v0.3.5. The same spelling `type` does not connect OKF concept kind to IMPACTS Core work-unit kind. The audit's `status: stable` is scoped to its dated evaluation. A changed meaning follows [Author or change](../../02_protocol/ontology.md#author-or-change) with valid, missing/conflicting and forbidden-inference cases; existing meaning follows [Use](../../02_protocol/ontology.md#use) at the actual source revision.
