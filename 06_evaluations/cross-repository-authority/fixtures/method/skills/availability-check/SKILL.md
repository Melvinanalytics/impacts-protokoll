---
name: availability-check
description: Produce availability evidence for a named case and date.
---

# Availability check

Address: [source namespace `urn:impacts:example:cross-repository:method`](../../CONTEXT.md); stable local operation key `availability-check` from frontmatter `name`; location this file; applicable revision `m4`.

This separate operation produces `availability_evidence` for a specific case and date. The fixture reports no validated availability evidence for the proposed offer. Its result cannot be inferred from the estimator's `status: ok`.
