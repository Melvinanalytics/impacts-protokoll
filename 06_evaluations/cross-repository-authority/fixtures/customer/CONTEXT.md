# Customer folder entry

Stable source namespace: `urn:impacts:example:cross-repository:customer`. Captured symbolic revision: `c8`. The shorthand `customer@c8` names this fixture and revision for the reading case; neither part is the stable source identity. This area maintains stable definitions; no accumulating instances are reported. Route the delivery question to the [delivery-promise definition](grundlagen/lieferzusage.md#definition). Its case inputs are reported, and availability proof is absent.
