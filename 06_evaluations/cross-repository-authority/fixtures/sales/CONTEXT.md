# Sales folder entry

Stable source namespace: `urn:impacts:example:cross-repository:sales`. Captured symbolic revision: `s3`. The shorthand `sales@s3` names this fixture and revision for the reading case; neither part is the stable source identity. A repeatable offer path accepts either a checked delivery commitment or a stated blocker. The [delivery rule](grundlagen/offer-terms.md#delivery) governs that result. The current folder router pins protocol `v0.3.4`; a proposed future route uses `v0.3.5` but has not been checked. A historical Core Run remains bound to its Application and inputs under `v0.3.3`.
