# Existing folder authority walk

This is a synthetic reading case, not a customer record or Core Application. The three folders below represent separate Git roots with symbolic revisions. Each entry router declares its synthetic stable source namespace. Start from those routers. Resolve source namespace, item identity, location and applicable revision before opening the [worked disposition](EXPECTED.md). These files do not prove a real installation, migration or business result.

- [Customer folder entry](fixtures/customer/CONTEXT.md)
- [Method folder entry](fixtures/method/CONTEXT.md)
- [Sales folder entry](fixtures/sales/CONTEXT.md)

Proposal: move the method folder's `delivery-estimate` item from `skills/delivery-estimate/SKILL.md` at revision `m4` to `skills/lead-time/SKILL.md` at candidate revision `m5`, retaining the method source namespace and item key. That identity continuity is proposed, not established. Identical bytes are claimed but unverified. One known customer link can change. An identified outside scheduled job may use the old path; its owner and configuration are unverified. In a second variant, a declared bounded search finds no specific outside lead, although other consumers remain possible.

Use the [protocol root router](../../CONTEXT.md), [Restructure mode](../../02_protocol/impacts-architect/SKILL.md#restructure-mode) and the applicable ontology route. For the metadata question, compare the [OKF concept `type`](https://github.com/GoogleCloudPlatform/open-knowledge-format/blob/main/SPEC.md#41-frontmatter) with the [IMPACTS workstep `type`](../../02_protocol/schemas/arbeitsschritt.schema.json). Record the actual paths read and the symbolic revision each fixture declares. Distinguish expected checks from checks performed.

1. Which form fits each folder? Does the proposal earn a new Core Application?
2. For the delivery-promise meaning, estimator, availability check and offer-commitment rule, what are the source namespace, item key, resolvable location and applicable revision? Where is each relationship maintained? Which consumers link, install or execute each source? What search coverage remains open?
3. Give the old/new migration map, consumer checks, safe retirement condition and recovery path for both outside-consumer variants. What independent work can proceed while the identified job remains unresolved?
4. Can an estimator `status: ok` justify an offer commitment? Can a check against an older protocol pin certify new use or change a historical Run?
5. Do the OKF and IMPACTS `type` fields or a dated audit's `status: stable` share meaning? Which source and ontology path govern a changed interpretation?

Write answers before opening [EXPECTED.md](EXPECTED.md). The identified outside job is a deliberate negative case; residual uncertainty is the nearby exception. The case does not execute a link, hash, validator or customer decision.
